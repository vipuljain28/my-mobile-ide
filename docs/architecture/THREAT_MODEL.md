# Threat model (Phase 08)

Assets: workspace code, session tokens, runtime lifecycle, audit logs.

Mitigations: path isolation, command policy, phase-gated agent, expiring sessions, HTTPS/WSS, rate/job limits, hashed audit ids, no extra runtime deps.
