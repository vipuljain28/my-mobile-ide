# PHASE 06 — Terminal + Git + Build + Test

## Status

**COMPLETE**

Commands run only under argv policy inside the workspace. No unrestricted host execution.

```bash
cd packages/dev-workflows
node --test test/*.test.js
```
