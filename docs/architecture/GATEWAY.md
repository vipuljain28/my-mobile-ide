# Remote Development Gateway (Phase 04)

## Role

The gateway is the **security boundary** between the mobile IDE client and the development runtime / workspace filesystem.

Transport: HTTPS + WSS only. No process execution API.

See `gateway/` for implementation.
